name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "pnpm exec prisma generate"
dev = "pnpm exec prisma db execute --file prisma/session.sql --schema prisma/schema.prisma && pnpm exec react-router dev"
